class Beamlet(object):
    def __init__(self, id, local_id, angle, position, realPosition):
        self.id = id
        self.local_id = local_id
        self.angle = angle
        self.position = position
        self.realPosition = realPosition

